# Generated by circleci config generate
version: "2.1"
jobs:
  build:
    docker:
      - image: cimg/node:26.7.0
    steps:
      - checkout
      - run:
          name: system
          command: curl -fsSL https://deb.nodesource.com/setup_26.x | sudo -E bash - && sudo apt-get install -y nodejs --no-install-recommends && sudo rm -rf /var/lib/apt/lists/* && sudo npm install -g pnpm
      - run:
          name: install
          command: pnpm install
      - run:
          name: test
          command: pnpm test
workflows:
  build:
    jobs:
      - build
